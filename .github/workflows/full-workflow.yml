# This workflow will do a clean install of node dependencies, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Complete workflow

on: ['push', 'pull_request']

jobs:

  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]
        mongodb-version: [4.2]

    steps:
    - uses: actions/checkout@v2
    - run: git fetch --prune --unshallow

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}

    - name: Start MongoDB
      uses: supercharge/mongodb-github-action@1.1.0
      with:
        mongodb-version: ${{ matrix.mongodb-version }}

    - name: Install dependencies
      run: yarn --frozen-lockfile

    - name: Run all tests
      run: yarn test

    #- shell: bash
      #if: github.ref == 'refs/heads/master'
      #name: Create SSH deployment key
      #env:
        #SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      #run: ./scripts/deploy.sh

    #- name: Deployment
      #if: github.ref == 'refs/heads/master'
      #run: git remote add umbler ssh://git@geonosis.deploy.umbler.com:9922/jy4xooxj/waldemarnt2-com.git && git push umbler master
      #env:
        #CI: true

    - uses: akhileshns/heroku-deploy@v3.12.12
      with:
        heroku_api_key: ${{secrets.HEROKU_API_KEY}}
        heroku_app_name: "peaceful-plateau-04699"
        heroku_email: "waldemarnt@gmail.com"
        buildpack: "https://github.com/zidizei/heroku-buildpack-tsc#v2.0"
